#!/usr/bin/make -f

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

export SETUPTOOLS_SCM_PRETEND_VERSION=8.1.0
export PYBUILD_NAME=jaraco.packaging

%:
	dh $@ --with python3 --buildsys=pybuild --test-pytest
